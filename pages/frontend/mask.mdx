## NPM
```css filename="install"  copy
pnpm i react-imask
```

## Example Below
import { IMaskInput } from "react-imask";
import styles from './mask.module.css'

export function ImaskCustomInput() {
  return (
    <div>
      <IMaskInput placeholder="500 00 00 00" mask="500 00 00 00" className={styles.imask}/>
    </div>
  );
}

 <ImaskCustomInput /> 

## Custom Imask
```css filename="Custom Use Example Imask"  copy
<IMaskInput
mask='500 00 00 00'
/>
```
## Mantine Imask
```css filename="Mantine Use Case"  copy
<InputBase
component={IMaskInput}
mask="000 00 00 00"
icon={<div>+995</div>}
/>
```

## Material UI Imask
```css filename="Material Ui Use Case"  copy
empty
```