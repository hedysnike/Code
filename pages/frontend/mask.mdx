         
```css filename="install"  copy
pnpm i react-imask
```

import { IMaskInput } from "react-imask";
import styles from './imask.module.css'

export function ImaskCustomInput() {
  return (
    <div className="">
      <IMaskInput mask="500 00 00 00" className={styles.imask}/>
    </div>
  );
}

  return <ImaskCustomInput />
          500 00 00 00 


```css filename="Custom Use Example Imask"  copy
<IMaskInput
mask='500 00 00 00'
/>
```
         
```css filename="Mantine Use Case"  copy
<InputBase
component={IMaskInput}
mask="000 00 00 00"
icon={<div>+995</div>}
/>
```


```css filename="Material Ui Use Case"  copy
empty
```